# Introduction

This chapter is an overview of the methods that we propose to solve an **important problem**.

```{r message=FALSE}
library(ggplot2)

ggplot(mtcars, aes(disp)) +
  geom_histogram()
```

but github pages won't find the output.